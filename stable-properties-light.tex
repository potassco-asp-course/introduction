% ----------------------------------------------------------------------
\begin{frame}{About \alt<3>{answer set}{stable model}s}
  \bigskip
  \begin{itemize}
  \item An \alt<3>{answer set}{stable model} is a model of a logic program
    \smallskip
  \item An \alt<3>{answer set}{stable model} is a subset of the head atoms of all rules\par in a logic program
    \smallskip
  \item A logic program can have zero, one, or (exponentially)\par many \alt<3>{answer set}{stable model}s
    \smallskip
  \item Two \alt<3>{answer set}{stable model}s of the same logic program are never\par contained in each other
    \footnote{This becomes untrue when considering choices}
    \bigskip
  \item<2-> \structure{Note} \ \alt<3>{answer set}{stable model}s may emerge when adding rules
  \end{itemize}
\end{frame}
% ----------------------------------------------------------------------
%
%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../../main"
%%% End:
