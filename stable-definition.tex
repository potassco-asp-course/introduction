% ----------------------------------------------------------------------
\begin{frame}{\alt<7->{Stable models}{What is the meaning of a logic program?\only<6>{\\
        Stable models!}}}

  \begin{itemize}
  \item <2-11> The \alert{reduct}, \reduct{P}{X}, of a program $P$ relative to
    a set $X$ of atoms is
    \begin{align*}
      \reduct{P}{X} &= \{\head{r}\leftarrow\pbody{r} \mid r\in P,\ \nbody{r}\cap X=\emptyset\}
    \end{align*}

  \item<only@3-4> \structure{Note} \ \reduct{P}{X} is obtained from $P$ by
    \begin{enumerate}\normalsize
    \item deleting each rule $r$ satisfying $\nbody{r}\cap X\neq\emptyset$

      and then

    \item deleting all negative body literals from the remaining rules
    \end{enumerate}
  \item<only@4>[] Only negative body literals are evaluated!

  \item <only@7-11> A set $X$ of atoms is a \alert{stable model} of a program $P$

    if $\Cn{\reduct{P}{X}}=X$

    \smallskip

  \item<only@8> \structure{Note}
    \begin{itemize}\normalsize
    \item $\Cn{\reduct{P}{X}}$ is the $\subseteq$-smallest model of \reduct{P}{X}
      \smallskip
    \item Each atom in $X$ is justified by a proof from \reduct{P}{X}
    \end{itemize}

  \item<only@9-10> \structure{Procedural counterpart}

  \item<only@10> []\small \textbf{guess} $Y$
  \item<only@10> []\small \textbf{let} $\T{\reduct{P}{Y}}{X}=\{\head{r}\mid r\in \reduct{P}{Y}, \pbody{r}\subseteq X\}$ \textbf{in}
    \begin{itemize}
    \item []$X := \emptyset$
      \smallskip
    \item []\textbf{while} {$\T{\reduct{P}{Y}}{X}\neq X$}
      \begin{itemize}\small
      \item[] $X := {\,\T{\reduct{P}{Y}}{X}}$
      \end{itemize}
    \item[]\small \textbf{if} $X=Y$ \textbf{then} \textbf{return} $X$ \textbf{else} \textbf{fail}
    \end{itemize}

  \end{itemize}
\end{frame}
% ----------------------------------------------------------------------
%
%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../../main"
%%% End:
